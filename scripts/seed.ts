/**
 * Seed script for initial data.
 * Usage: pnpm seed
 */

import * as dotenv from 'dotenv';
dotenv.config();

async function main() {
  console.log('Starting seed...');
  console.log('Add your seed data logic here.');
  console.log('Seed completed.');
}

main().catch(console.error);
